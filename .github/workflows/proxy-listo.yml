name: 🛡️ Proxy AntiBan OK
on:
  workflow_dispatch

jobs:
  correr:
    runs-on: ubuntu-latest
    steps:
      - name: Instalar
        run: |
          sudo apt update -y
          sudo apt install -y wget curl
          wget -q https://raw.githubusercontent.com/serjs/socks5-server/master/install.sh
          chmod +x install.sh
          sudo ./install.sh -u ADM -p JEICO777 -P 8888 --anti-detect
          echo "TU IP:"
          curl ifconfig.me
